{{ if eq .config.xorg true -}}
#!/bin/sh

TDM="$(command -v tdm)"

if [ -d /etc/X11/xinit/xinitrc.d ]; then
  for f in /etc/X11/xinit/xinitrc.d/?*.sh; do
    [ -x "$f" ] && . "$f"
  done
  unset f
fi

if [ -n "$TDM" ]; then
  exec "$TDM" --xstart
fi
{{- end }}
